import * as React from 'react';

const NavBar: React.SFC = props => {
  return (
    <div
      className="h3 pt4 tc"
      style={{ backgroundColor: '#1b5236', color: '#FFFFFF' }}
    >
      TD Smart Rewards
    </div>
  );
};

export default NavBar;
