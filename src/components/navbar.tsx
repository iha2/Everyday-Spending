import * as React from 'react';

const NavBar: React.SFC = props => {
  return (
    <div className="h4" style={{ backgroundColor: '#1b5236' }}>
      NavBar
    </div>
  );
};

export default NavBar;
